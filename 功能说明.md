# QuotaMonitor 功能说明

## 软件简介

QuotaMonitor（AI 额度监控）是一款 Windows 桌面小工具，用于**实时查看和管理 DeepSeek API 余额与 OpenCode Go 套餐余量**。它会在桌面显示一个常驻悬浮窗，随时更新当前额度；额度低于预警线或消费出现异常时，会自动弹出系统通知提醒你；同时持续记录消费历史，帮你分析额度消耗趋势、估算还能用多久。

---

## 一、桌面悬浮窗

悬浮窗是软件最主要的显示载体，始终浮在桌面最上层，一眼就能看到当前额度。

### 额度显示与状态颜色

- 悬浮窗实时显示当前额度（如 ¥123.45），支持拖动到屏幕任意位置，位置会被记住，下次启动自动复原。
- 颜色随查询状态自动变化：
  - 🟢 **绿色** — 状态正常
  - 🔴 **红色** — 余额不足（低于预警阈值）
  - 🟠 **橙色** — 查询出错时显示，此时窗口保留最后一次成功获取的额度并带有 ⚠ 警示标记
- 鼠标悬停在悬浮窗上会显示详细状态说明（如错误原因）。

### 两种监控模式

- **余额模式**：显示 DeepSeek 账户余额。
- **套餐模式**：显示 OpenCode Go 套餐余量，包含**5 小时 / 周 / 月**三个额度窗口；单击悬浮窗即可循环切换显示哪个窗口（5 小时 → 周 → 月 → 5 小时 …）。
- 可在设置中切换套餐窗口显示**剩余额度**或**已用额度**。
- 右键菜单提供「切换到 Go 套餐 / 切换到余额」，随时切换监控模式。

### 交互操作

| 操作 | 作用 |
|------|------|
| 左键拖动 | 移动悬浮窗位置（松手自动记住） |
| 左键单击 | 套餐模式下循环切换额度窗口（5 小时 / 周 / 月） |
| 双击 | 打开「统计」面板 |
| 右键 | 弹出菜单：切换到余额 / Go 套餐、设置 / 隐藏 / 退出 |

### 显示效果

- **悬停增亮**：鼠标移上去时窗口变亮，移开后自动变淡，两档亮度可分别调整，减少屏幕干扰。
- **锁定模式（点击穿透）**：开启后鼠标事件直接穿透悬浮窗，不影响你对窗口下方内容的任何点击操作，但额度依然清晰可见。
- **置顶开关**：可随时取消或恢复"始终置顶"，防止被其他窗口盖住。

---

## 二、定时隐藏

觉得悬浮窗碍事时，可以让它"消失"一段时间，到点自动恢复：

- 右键悬浮窗 → 隐藏，选择**预设时长**（5 分钟 / 30 分钟 / 3 小时）或**自定义时长**（1～600 分钟任意值）。
- 隐藏期间托盘菜单顶部会出现「显示」按钮，可随时提前恢复。
- 重复设置隐藏会**重新计时**；隐藏到期自动恢复显示。
- 隐藏时会收到一条系统通知，告知恢复时间。

---

## 三、系统托盘

软件常驻系统托盘（任务栏右下角通知区域），关闭窗口不会退出程序。

- **托盘图标**：圆形图标，颜色随状态变化——靛蓝（默认/无数据）/ 绿色（充足）/ 红色（不足）/ 橙色（异常）。
- **悬停气泡**：显示当前余额；出错时显示错误原因和连续失败次数。
- **左键单击**：打开「统计」面板；若悬浮窗处于隐藏状态，则先恢复显示。
- **右键菜单**：
  - 显示（仅隐藏时出现）
  - 当前余额（只读显示）
  - 错误信息（只读显示，含连续失败次数）
  - 设置...
  - 复位（把悬浮窗移回屏幕中央）
  - 锁定 / 解锁（点击穿透开关）
  - 置顶 / 取消置顶
  - 统计
  - 退出（唯一真正退出程序的方式）

---

## 四、智能告警

### 余额不足提醒 ⚠

- 当余额跌到**预警阈值**以下时，弹出系统通知：「⚠ 余额不足——DeepSeek 余额仅剩 ¥xx（预警阈值: ¥xx）」。
- 只在余额从正常转为不足的**临界点提醒一次**，余额恢复后会自动重置，不会反复打扰。

### 消费突增提醒 🔥

- 当**今日消费金额超过近 7 天日均消费的 3 倍**时，弹出「🔥 消费突增」通知，提示及时排查异常消耗。
- 每天最多提醒一次。

以上两类通知均可在设置中整体开关。

---

## 五、额度统计与历史记录

双击悬浮窗或点击托盘「统计」，打开统计面板，包含：

### 统计摘要

- 当前余额
- 日均消费、月均消费
- 预计可用天数（按整体日均推算）
- 今日消费
- 近 7 天日均消费、按近 7 天消费水平预计可用天数

### 余额走势图

- 折线图展示余额变化趋势，包含网格参考线。
- 图上标出**预警阈值虚线**（红色，标注金额），一眼看出余额何时跌破预警线。
- 标注起点和终点的具体余额数值与日期。

### 历史记录表

- 按时间倒序展示全部余额记录（时间 / 余额 / 变动）。
- 每次余额变动自动计算差额：**下降标红、上升标绿**，消费明细一目了然。
- 相同余额自动去重，不产生重复记录。

### 数据导出

- 一键导出完整历史记录为 CSV 表格文件，可用 Excel 打开查看。

---

## 六、软件设置

设置窗口采用**左侧导航 + 内容区**布局，共 4 页。所有设置**改动即时生效**，无需重启软件。

### 显示

| 设置项 | 说明 |
|--------|------|
| 字体大小 | 调节悬浮窗文字大小（12～48），拖动滑杆即时预览 |
| 整体透明度 | 悬浮窗整体透明度（30%～100%） |
| 鼠标离开暗度 | 鼠标移开后窗口变暗的程度（10%～100%） |
| 套餐窗口显示剩余额度 | 开启显示剩余额度，关闭则显示已用额度 |

### 提醒

| 设置项 | 说明 |
|--------|------|
| 预警阈值 | 设置余额预警线（¥ 0～99999.99），改动后悬浮窗颜色立即更新 |
| 余额低于阈值时弹出通知 | 余额不足提醒开关 |
| 消费突增时弹出通知 | 消费突增提醒开关 |

### 密钥

分 **DeepSeek API** 与 **OpenCode Go API** 两个区块，各自独立管理：

- 密码框输入密钥，默认隐藏显示（可勾选「显示」明文查看）。
- **测试**：一键验证密钥是否有效，结果显示成功或失败原因。
- **应用**：确认弹窗（显示密钥末 4 位便于核对）后立即生效，**马上用新密钥重新获取额度**，无需等待下一个刷新周期。
- **清空**：二次确认后停止查询该接口。

### 其他

| 设置项 | 说明 |
|--------|------|
| 刷新间隔 | 额度刷新频率（5 秒 / 15 秒 / 30 秒 / 1 分钟 / 1 分 30 秒 / 2 分钟） |
| 开机自启 | 登录 Windows 后自动启动软件 |

窗口底部提供「打开数据文件夹」按钮，可直接跳转到数据目录（`文档\QuotaMonitor\`）。

---

## 七、数据保存位置

软件自动创建并维护以下数据（位于 Windows 用户目录 `文档\QuotaMonitor\` 下）：

- **设置.json**：保存所有设置项，密钥加密存储，安全可靠。
- **余额记录.json**：持续累积的余额变化记录，供统计面板展示与导出。
- **日志**：记录软件运行情况，便于排查问题（保留最近若干份，不会无限膨胀）。

> 旧版本（DeepSeek 余额监控）的数据存放在 `文档\DeepSeek余额监控\`，升级后会**自动迁移**到新目录，历史记录不丢失。

---

*本文档仅介绍软件功能，不涉及任何技术实现细节。*
