# QuotaMonitor 功能说明

## 软件简介

QuotaMonitor是一款 Windows 桌面小工具，用于**实时查看和管理 DeepSeek API 账户余额**。它会在桌面显示一个常驻悬浮窗，随时更新当前余额；余额低于预警线或消费出现异常时，会自动弹出系统通知提醒你；同时持续记录消费历史，帮你分析余额消耗趋势、估算还能用多久。

---

## 一、桌面悬浮窗

悬浮窗是软件最主要的显示载体，始终浮在桌面最上层，一眼就能看到当前余额。

### 余额显示与状态颜色

- 悬浮窗实时显示当前余额（如 ¥123.45），支持拖动到屏幕任意位置，位置会被记住，下次启动自动复原。
- 颜色随余额状态自动变化：
  - 🟢 **绿色** — 余额充足（高于预警阈值）
  - 🔴 **红色** — 余额不足（低于预警阈值）
  - 🟠 **橙色** — 查询出错时显示，此时窗口保留最后一次成功获取的余额并带有 ⚠ 警示标记
- 鼠标悬停在悬浮窗上会显示详细状态说明（如错误原因）。

### 交互操作

| 操作 | 作用 |
|------|------|
| 左键拖动 | 移动悬浮窗位置（松手自动记住） |
| 双击 | 打开「统计」面板 |
| 右键 | 弹出菜单：设置 / 隐藏 / 退出 |

### 显示效果

- **悬停增亮**：鼠标移上去时窗口变亮，移开后自动变淡，两档亮度可分别调整，减少屏幕干扰。
- **锁定模式（点击穿透）**：开启后鼠标事件直接穿透悬浮窗，不影响你对窗口下方内容的任何点击操作，但余额依然清晰可见。
- **置顶开关**：可随时取消或恢复"始终置顶"，防止被其他窗口盖住。

---

## 二、定时隐藏

觉得悬浮窗碍事时，可以让它"消失"一段时间，到点自动恢复：

- 右键悬浮窗 → 隐藏，选择**预设时长**（5 分钟 / 30 分钟 / 3 小时）或**自定义时长**（1～600 分钟任意值）。
- 隐藏期间托盘菜单顶部会出现「显示」按钮，可随时提前恢复。
- 重复设置隐藏会**重新计时**；隐藏到期自动恢复显示。
- 隐藏时会收到一条系统通知，告知恢复时间。

---

## 三、系统托盘

软件常驻系统托盘（任务栏右下角通知区域），关闭窗口不会退出程序。

- **托盘图标**：圆形 "D" 图标，颜色随余额状态变化——绿色（充足）/ 红色（低于阈值）/ 橙色（异常）/ 靛蓝色（默认）。
- **悬停气泡**：显示当前余额；出错时显示错误原因和连续失败次数。
- **左键单击**：打开「统计」面板；若悬浮窗处于隐藏状态，则先恢复显示。
- **右键菜单**：
  - 显示（仅隐藏时出现）
  - 当前余额（只读显示）
  - 错误信息（只读显示，含连续失败次数）
  - 设置...
  - 复位（把悬浮窗移回屏幕中央）
  - 锁定 / 解锁（点击穿透开关）
  - 置顶 / 取消置顶
  - 统计
  - 退出（唯一真正退出程序的方式）

---

## 四、智能告警

### 余额不足提醒 ⚠

- 当余额跌到**预警阈值**以下时，弹出系统通知：「⚠ 余额不足——DeepSeek 余额仅剩 ¥xx（预警阈值: ¥xx）」。
- 只在余额从正常转为不足的**临界点提醒一次**，余额恢复后会自动重置，不会反复打扰。

### 消费突增提醒 🔥

- 当**今日消费金额超过近 7 天日均消费的 3 倍**时，弹出「🔥 消费突增」通知，提示及时排查异常消耗。
- 每天最多提醒一次。

以上两类通知均可在设置中整体开关。

---

## 五、余额统计与历史记录

双击悬浮窗或点击托盘「统计」，打开统计面板，包含：

### 统计摘要

- 当前余额
- 日均消费、月均消费
- 预计可用天数（按整体日均推算）
- 今日消费
- 近 7 天日均消费、按近 7 天消费水平预计可用天数

### 余额走势图

- 折线图展示余额变化趋势，包含网格参考线。
- 图上标出**预警阈值虚线**（红色，标注金额），一眼看出余额何时跌破预警线。
- 标注起点和终点的具体余额数值与日期。

### 历史记录表

- 按时间倒序展示全部余额记录（时间 / 余额 / 变动）。
- 每次余额变动自动计算差额：**下降标红、上升标绿**，消费明细一目了然。
- 相同余额自动去重，不产生重复记录。

### 数据导出

- 一键导出完整历史记录为 CSV 表格文件，可用 Excel 打开查看。

---

## 六、软件设置

所有设置**改动即时生效**，无需重启软件。包含以下项目：

| 设置项 | 说明 |
|--------|------|
| 字体大小 | 调节悬浮窗文字大小（12～48），拖动滑杆即时预览 |
| 透明度 | 悬浮窗整体透明度（30%～100%） |
| 鼠标离开透明度 | 鼠标移开后窗口变暗的程度（10%～100%） |
| 预警阈值 | 设置余额预警线（¥ 0～99999.99），改动后悬浮窗颜色立即更新 |
| 通知开关 | 余额低于阈值时是否弹出通知 |
| API 密钥 | 管理 DeepSeek 账户密钥，见下方说明 |
| 刷新间隔 | 余额刷新频率（5 秒～2 分钟，六档可选） |
| 开机自启 | 登录 Windows 后自动启动软件 |

### API 密钥管理

- 密码框输入密钥，默认隐藏显示。
- **测试**：一键验证密钥是否有效，结果显示成功或失败原因。
- **应用**：确认弹窗（显示密钥末 4 位便于核对）后立即生效，**马上用新密钥重新获取余额**，无需等待下一个刷新周期。
- 也支持清空密钥（二次确认后停止查询余额）。

---

## 七、数据保存位置

软件自动创建并维护以下数据（位于 Windows 用户目录下）：

- **配置文件**：保存所有设置项，密钥加密存储，安全可靠。
- **余额历史记录**：持续累积的余额变化记录，供统计面板展示与导出。
- **运行日志**：记录软件运行情况，便于排查问题（保留最近若干份，不会无限膨胀）。

---

*本文档仅介绍软件功能，不涉及任何技术实现细节。*
