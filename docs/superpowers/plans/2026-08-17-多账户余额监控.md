# 多账户余额监控 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 DeepSeek 余额监控从单账户扩展为多供应商多账户并行监控（DeepSeek / OpenRouter / Moonshot / Z.ai），托盘菜单切换当前账户，历史与统计按账户隔离。

**Architecture:** 参照 dsh-usage-stats 的适配器方案：新增 `IBalanceProvider` 适配器接口 + `ProviderRegistry` 注册表，各供应商实现统一归一化余额模型 `AccountBalance`；`Config` 从单 `ApiKey` 改为 `List<AccountConfig>`；每账户一个 `BalanceMonitor` 实例，由 `MonitorCoordinator` 管理，悬浮窗/托盘/告警/统计按 `ActiveAccountId` 取当前账户。

**Tech Stack:** C# / WinForms / .NET Framework 4.8（SDK 风格 csproj，LangVersion 9.0）、JavaScriptSerializer、DPAPI、零新依赖。

**Spec:** `docs/superpowers/specs/2026-08-17-多账户余额监控-design.md`

## Global Constraints

- net48 + LangVersion 9.0：不能用 record、required、file-scoped namespace 等 C# 10+ 语法；namespace 用块作用域。
- 不新增 NuGet 包；JSON 继续用 `System.Web.Script.Serialization.JavaScriptSerializer`。
- 中文注释，沿用现有代码风格（每个类 XML 注释）。
- 数据存储目录不变（`Documents\DeepSeek余额监控\`）；API 密钥只以 DPAPI 密文落盘。
- 构建：`dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release`
- 测试：`dotnet build tools/CoreTests/CoreTests.csproj -c Release` 后运行 `tools\CoreTests\bin\Release\net48\CoreTests.exe`（返回 0 为通过）。
- 每任务结束必须可编译 + 相关测试通过 + 提交。
- 设计决策调整（比 spec 更简）：**保留每账户各自的 `Timer` 轮询**（BalanceMonitor 现有退避/防重入逻辑原样复用），`MonitorCoordinator` 只负责管理列表、聚合事件、广播间隔，不做单定时器集中调度。理由：零改动复用现有轮询逻辑，账户数少（<20），改动面最小。

---

### Task 1: 适配器层核心 + DeepSeek 适配器

**Files:**
- Create: `src/DeepSeekBalanceMonitor/Core/Providers/AccountBalance.cs`
- Create: `src/DeepSeekBalanceMonitor/Core/Providers/IBalanceProvider.cs`
- Create: `src/DeepSeekBalanceMonitor/Core/Providers/DeepSeekProvider.cs`
- Create: `src/DeepSeekBalanceMonitor/Core/Providers/ProviderRegistry.cs`
- Create: `src/DeepSeekBalanceMonitor/Core/Providers/BalanceQueryException.cs`（把 `QueryErrorKind`/`BalanceQueryException` 从 `DeepSeekApiClient.cs` 移到这里，供所有适配器共用）
- Modify: `src/DeepSeekBalanceMonitor/Core/DeepSeekApiClient.cs`（删除其中 `QueryErrorKind`/`BalanceQueryException` 定义；本任务保留该类本身，Task 8 再删）
- Test: `tools/CoreTests/Program.cs`

**Interfaces:**
- Consumes: 无
- Produces:
  - `AccountBalance { bool IsAvailable; decimal? Remaining; decimal? Used; decimal? Total; decimal? Granted; decimal? ToppedUp; string Currency; }`
  - `interface IBalanceProvider { string Id; string DisplayName; string BaseUrl; Task<AccountBalance> GetBalanceAsync(string apiKey); }`
  - `DeepSeekProvider : IBalanceProvider` + `static AccountBalance ParseBalance(string body)`（公开供测试）
  - `static class ProviderRegistry { Register(IBalanceProvider); IReadOnlyList<IBalanceProvider> All { get; } IBalanceProvider Get(string id); }`

- [ ] **Step 1: 写失败测试** — 在 `tools/CoreTests/Program.cs` 的 `Main()` 中加入 `TestDeepSeekProvider();`，新增测试方法（DeepSeek 解析逻辑从现有 `TestBalanceParse` 迁移为针对 `DeepSeekProvider.ParseBalance`）：

```csharp
private static void TestDeepSeekProvider()
{
    Console.WriteLine("-- DeepSeek 适配器 --");
    var r1 = DeepSeekProvider.ParseBalance(
        "{\"is_available\":true,\"balance_infos\":[{\"currency\":\"CNY\",\"total_balance\":\"110.00\",\"granted_balance\":\"10.00\",\"topped_up_balance\":\"100.00\"}]}");
    Assert(r1.Remaining == 110m && r1.IsAvailable, "标准 CNY 响应解析");
    Assert(r1.Granted == 10m && r1.ToppedUp == 100m && r1.Currency == "CNY", "赠送/充值/币种解析");

    var r2 = DeepSeekProvider.ParseBalance(
        "{\"is_available\":true,\"balance_infos\":[{\"currency\":\"USD\",\"total_balance\":\"15.50\"},{\"currency\":\"CNY\",\"total_balance\":\"88.88\"}]}");
    Assert(r2.Remaining == 88.88m, "多币种取 CNY");

    var r3 = DeepSeekProvider.ParseBalance(
        "{\"is_available\":true,\"balance_infos\":[{\"currency\":\"USD\",\"total_balance\":\"15.50\"}]}");
    Assert(r3.Remaining == 15.50m && r3.Currency == "USD", "无 CNY 取首个币种");

    AssertThrows<FormatException>(() => DeepSeekProvider.ParseBalance(
        "{\"is_available\":true,\"balance_infos\":[]}"), "空余额列表抛异常");
    AssertThrowsAny(() => DeepSeekProvider.ParseBalance("not json"), "非法 JSON 抛异常");

    Assert(ProviderRegistry.Get("deepseek") != null, "注册表包含 deepseek");
    Assert(ProviderRegistry.All.Count >= 1, "注册表 All 非空");
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 编译失败（`DeepSeekProvider`/`ProviderRegistry` 不存在）。

- [ ] **Step 3: 实现适配器层**

`AccountBalance.cs`:

```csharp
using System;

namespace DeepSeekBalanceMonitor.Core
{
    /// <summary>归一化账户余额（多供应商统一结构）。各适配器把原始响应解析为本结构。</summary>
    public class AccountBalance
    {
        /// <summary>账户是否可调用 API。</summary>
        public bool IsAvailable { get; set; }

        /// <summary>剩余可用余额。</summary>
        public decimal? Remaining { get; set; }

        /// <summary>已用（部分供应商提供）。</summary>
        public decimal? Used { get; set; }

        /// <summary>总额度（部分供应商提供）。</summary>
        public decimal? Total { get; set; }

        /// <summary>赠送额度。</summary>
        public decimal? Granted { get; set; }

        /// <summary>充值额度。</summary>
        public decimal? ToppedUp { get; set; }

        /// <summary>币种（默认 CNY）。</summary>
        public string Currency { get; set; } = "CNY";
    }
}
```

`IBalanceProvider.cs`:

```csharp
using System.Threading.Tasks;

namespace DeepSeekBalanceMonitor.Core
{
    /// <summary>余额供应商适配器：把某家供应商的余额接口统一为一个查询方法。</summary>
    public interface IBalanceProvider
    {
        /// <summary>供应商唯一标识（如 "deepseek"）。</summary>
        string Id { get; }

        /// <summary>显示名（如 "DeepSeek"）。</summary>
        string DisplayName { get; }

        /// <summary>接口基址。</summary>
        string BaseUrl { get; }

        /// <summary>查询余额。失败时抛 <see cref="BalanceQueryException"/>。</summary>
        Task<AccountBalance> GetBalanceAsync(string apiKey);
    }
}
```

`DeepSeekProvider.cs`（从 `DeepSeekApiClient` 迁移网络与解析逻辑，保留静态 `ParseBalance` 供测试）：

```csharp
using System;
using System.Globalization;
using System.Net;
using System.Net.Http;
using System.Threading.Tasks;
using System.Web.Script.Serialization;

namespace DeepSeekBalanceMonitor.Core
{
    /// <summary>
    /// DeepSeek 官方余额适配器。
    /// GET https://api.deepseek.com/user/balance
    /// Authorization: Bearer &lt;API Key&gt;
    /// </summary>
    public class DeepSeekProvider : IBalanceProvider
    {
        public string Id => "deepseek";
        public string DisplayName => "DeepSeek";
        public string BaseUrl => "https://api.deepseek.com";

        private const string Endpoint = "https://api.deepseek.com/user/balance";
        private static readonly HttpClient Http = new HttpClient { Timeout = TimeSpan.FromSeconds(10) };
        private static readonly JavaScriptSerializer Json = new JavaScriptSerializer();

        public async Task<AccountBalance> GetBalanceAsync(string apiKey)
        {
            if (string.IsNullOrWhiteSpace(apiKey))
                throw new BalanceQueryException(QueryErrorKind.AuthFailed, "尚未设置 API 密钥，请先到「设置」中填写");

            using (var req = new HttpRequestMessage(HttpMethod.Get, Endpoint))
            {
                req.Headers.TryAddWithoutValidation("Authorization", "Bearer " + apiKey.Trim());
                req.Headers.TryAddWithoutValidation("Accept", "application/json");

                HttpResponseMessage resp;
                try { resp = await Http.SendAsync(req).ConfigureAwait(false); }
                catch (Exception ex)
                {
                    throw new BalanceQueryException(QueryErrorKind.Network, "网络请求失败: " + ex.Message);
                }

                using (resp)
                {
                    var body = await resp.Content.ReadAsStringAsync().ConfigureAwait(false);
                    if (resp.StatusCode == HttpStatusCode.Unauthorized || resp.StatusCode == HttpStatusCode.Forbidden
                        || body.Contains("40002") || body.Contains("40003"))
                        throw new BalanceQueryException(QueryErrorKind.AuthFailed,
                            "API 密钥无效或已失效（HTTP " + (int)resp.StatusCode + "）");
                    if (!resp.IsSuccessStatusCode)
                        throw new BalanceQueryException(QueryErrorKind.Network,
                            "接口返回异常状态码 HTTP " + (int)resp.StatusCode);
                    try { return ParseBalance(body); }
                    catch (Exception ex)
                    {
                        throw new BalanceQueryException(QueryErrorKind.ParseError,
                            "无法解析余额数据（接口可能变更）: " + ex.Message);
                    }
                }
            }
        }

        /// <summary>解析官方响应，取人民币（CNY）余额。公开供测试。</summary>
        public static AccountBalance ParseBalance(string body)
        {
            var doc = Json.Deserialize<BalanceDoc>(body);
            if (doc == null || doc.balance_infos == null || doc.balance_infos.Length == 0)
                throw new FormatException("响应中无余额信息");

            BalanceInfo cny = null;
            foreach (var info in doc.balance_infos)
            {
                if (string.Equals(info.currency, "CNY", StringComparison.OrdinalIgnoreCase)) { cny = info; break; }
            }
            var picked = cny ?? doc.balance_infos[0];
            if (picked == null || string.IsNullOrEmpty(picked.total_balance))
                throw new FormatException("余额字段为空");

            return new AccountBalance
            {
                IsAvailable = doc.is_available,
                Remaining = ParseNullable(picked.total_balance),
                Granted = ParseNullable(picked.granted_balance),
                ToppedUp = ParseNullable(picked.topped_up_balance),
                Currency = picked.currency ?? "CNY"
            };
        }

        /// <summary>字符串余额 → decimal?；空串返回 null。</summary>
        private static decimal? ParseNullable(string value)
        {
            if (string.IsNullOrEmpty(value)) return null;
            return decimal.Parse(value, CultureInfo.InvariantCulture);
        }

        private class BalanceDoc
        {
            public bool is_available { get; set; }
            public BalanceInfo[] balance_infos { get; set; }
        }

        private class BalanceInfo
        {
            public string currency { get; set; }
            public string total_balance { get; set; }
            public string granted_balance { get; set; }
            public string topped_up_balance { get; set; }
        }
    }
}
```

`ProviderRegistry.cs`:

```csharp
using System;
using System.Collections.Generic;
using System.Linq;

namespace DeepSeekBalanceMonitor.Core
{
    /// <summary>余额供应商注册表：集中登记所有适配器，按 Id 查找。</summary>
    public static class ProviderRegistry
    {
        private static readonly Dictionary<string, IBalanceProvider> _providers =
            new Dictionary<string, IBalanceProvider>(StringComparer.OrdinalIgnoreCase);

        static ProviderRegistry()
        {
            Register(new DeepSeekProvider());
        }

        /// <summary>登记一个适配器（重复 Id 覆盖）。</summary>
        public static void Register(IBalanceProvider provider)
        {
            if (provider == null || string.IsNullOrEmpty(provider.Id)) return;
            _providers[provider.Id] = provider;
        }

        /// <summary>全部已登记的适配器。</summary>
        public static IReadOnlyList<IBalanceProvider> All => _providers.Values.ToList();

        /// <summary>按 Id 查找；不存在返回 null。</summary>
        public static IBalanceProvider Get(string id)
        {
            if (string.IsNullOrEmpty(id)) return null;
            return _providers.TryGetValue(id, out var p) ? p : null;
        }
    }
}
```

`BalanceQueryException.cs`（从 `DeepSeekApiClient.cs` 原样搬出，删除该文件中的原定义）：

```csharp
using System;

namespace DeepSeekBalanceMonitor.Core
{
    /// <summary>余额查询失败的原因分类（用于悬浮窗/托盘提示）。</summary>
    public enum QueryErrorKind
    {
        /// <summary>API 密钥无效或已失效（401 / 40002 / 40003 等认证错误）。</summary>
        AuthFailed,
        /// <summary>网络不通、超时、限流等。</summary>
        Network,
        /// <summary>接口返回了无法解析的内容（可能接口变更）。</summary>
        ParseError,
        /// <summary>账户不可用（欠费停用等）。</summary>
        AccountUnavailable
    }

    /// <summary>带错误分类的余额查询异常。</summary>
    public class BalanceQueryException : Exception
    {
        public QueryErrorKind Kind { get; }

        public BalanceQueryException(QueryErrorKind kind, string message) : base(message)
        {
            Kind = kind;
        }
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 全 PASS，退出码 0。

- [ ] **Step 5: 提交**

```bash
git add src/DeepSeekBalanceMonitor/Core/Providers tools/CoreTests/Program.cs
git commit -m "feat: 余额供应商适配器层 + DeepSeek 适配器"
```

---

### Task 2: OpenRouter / Moonshot / Z.ai 适配器

**Files:**
- Create: `src/DeepSeekBalanceMonitor/Core/Providers/OpenRouterProvider.cs`
- Create: `src/DeepSeekBalanceMonitor/Core/Providers/MoonshotProvider.cs`
- Create: `src/DeepSeekBalanceMonitor/Core/Providers/ZaiProvider.cs`
- Modify: `src/DeepSeekBalanceMonitor/Core/Providers/ProviderRegistry.cs`（static ctor 注册 3 家）
- Test: `tools/CoreTests/Program.cs`

**Interfaces:**
- Consumes: `IBalanceProvider`、`AccountBalance`、`BalanceQueryException`、`QueryErrorKind`、`ProviderRegistry`（Task 1）
- Produces: `OpenRouterProvider` / `MoonshotProvider` / `ZaiProvider`（均含公开静态 `ParseXxx(string body)` 供测试）

- [ ] **Step 1: 写失败测试** — `Main()` 中加 `TestOtherProviders();`：

```csharp
private static void TestOtherProviders()
{
    Console.WriteLine("-- OpenRouter / Moonshot / Z.ai 适配器 --");

    // OpenRouter：credits 接口（Management Key）
    var o = OpenRouterProvider.ParseCredits(
        "{\"data\":{\"total_credits\":100.0,\"total_usage\":23.5}}");
    Assert(o.Remaining == 76.5m && o.Used == 23.5m && o.Total == 100.0m && o.Currency == "USD", "OpenRouter 剩余=总额-已用");

    // Moonshot：available / voucher / cash
    var m = MoonshotProvider.ParseBalance(
        "{\"data\":{\"available_balance\":42.5,\"voucher_balance\":10.0,\"cash_balance\":32.5,\"currency\":\"CNY\"}}");
    Assert(m.Remaining == 42.5m && m.Granted == 10.0m && m.ToppedUp == 32.5m, "Moonshot 余额分解");

    // Z.ai：total / available
    var z = ZaiProvider.ParseBalance(
        "{\"data\":{\"total_balance\":200.0,\"available_balance\":188.5,\"currency\":\"CNY\"}}");
    Assert(z.Remaining == 188.5m && z.Total == 200.0m, "Z.ai 可用余额解析");

    // 注册表
    Assert(ProviderRegistry.Get("openrouter") != null, "注册表包含 openrouter");
    Assert(ProviderRegistry.Get("moonshot") != null, "注册表包含 moonshot");
    Assert(ProviderRegistry.Get("zai") != null, "注册表包含 zai");
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 编译失败（类不存在）。

- [ ] **Step 3: 实现 3 个适配器 + 注册**

三个文件共用同一 HTTP 模式（与 DeepSeekProvider 相同），仅 Endpoint 与解析不同。各解析方法直接接受 JSON 字符串返回 `AccountBalance`。

`OpenRouterProvider.cs` 核心：

```csharp
public class OpenRouterProvider : IBalanceProvider
{
    public string Id => "openrouter";
    public string DisplayName => "OpenRouter";
    public string BaseUrl => "https://openrouter.ai/api";

    private const string Endpoint = "https://openrouter.ai/api/v1/credits";

    // GetBalanceAsync 与 DeepSeekProvider 同构：GET Endpoint + Bearer；认证错误(401/403)→AuthFailed；非 2xx→Network；解析失败→ParseError

    /// <summary>解析 credits 响应：剩余 = total_credits - total_usage。</summary>
    public static AccountBalance ParseCredits(string body)
    {
        var doc = Json.Deserialize<CreditsDoc>(body);
        if (doc?.data == null)
            throw new FormatException("响应无 data 字段");
        var total = doc.data.total_credits;
        var used = doc.data.total_usage;
        if (total == null && used == null)
            throw new FormatException("响应无 credits 数据");
        return new AccountBalance
        {
            IsAvailable = total != null && used != null ? total - used > 0 : total > 0,
            Remaining = total != null && used != null ? total - used : total,
            Used = used,
            Total = total,
            Currency = "USD"
        };
    }

    private class CreditsDoc { public CreditsData data { get; set; } }
    private class CreditsData { public double? total_credits { get; set; } public double? total_usage { get; set; } }
}
```

> 注：JSON 数值用 `double?` 解析（JavaScriptSerializer 默认）；余额比较用 double 足够，入库前在 `BalanceMonitor` 处无精度需求。

`MoonshotProvider.cs` 核心（Endpoint `https://api.moonshot.cn/v1/users/me/balance`）：

```csharp
public static AccountBalance ParseBalance(string body)
{
    var doc = Json.Deserialize<BalanceDoc>(body);
    var d = doc?.data;
    if (d == null) throw new FormatException("响应无 data 字段");
    return new AccountBalance
    {
        IsAvailable = d.available_balance > 0,
        Remaining = (decimal?)d.available_balance,
        Granted = (decimal?)d.voucher_balance,
        ToppedUp = (decimal?)d.cash_balance,
        Currency = string.IsNullOrEmpty(d.currency) ? "CNY" : d.currency
    };
}
private class BalanceDoc { public BalanceData data { get; set; } }
private class BalanceData { public double available_balance { get; set; } public double voucher_balance { get; set; } public double cash_balance { get; set; } public string currency { get; set; } }
```

`ZaiProvider.cs` 核心（Endpoint `https://api.z.ai/api/paas/v4/balance`）：

```csharp
public static AccountBalance ParseBalance(string body)
{
    var doc = Json.Deserialize<BalanceDoc>(body);
    var d = doc?.data;
    if (d == null) throw new FormatException("响应无 data 字段");
    return new AccountBalance
    {
        IsAvailable = d.available_balance > 0,
        Remaining = (decimal?)d.available_balance,
        Total = (decimal?)d.total_balance,
        Currency = string.IsNullOrEmpty(d.currency) ? "CNY" : d.currency
    };
}
private class BalanceDoc { public BalanceData data { get; set; } }
private class BalanceData { public double total_balance { get; set; } public double available_balance { get; set; } public string currency { get; set; } }
```

`ProviderRegistry.cs` static ctor 追加：

```csharp
Register(new OpenRouterProvider());
Register(new MoonshotProvider());
Register(new ZaiProvider());
```

- [ ] **Step 4: 运行测试验证通过**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 全 PASS。

- [ ] **Step 5: 提交**

```bash
git add src/DeepSeekBalanceMonitor/Core/Providers
git commit -m "feat: OpenRouter/Moonshot/Z.ai 余额适配器"
```

---

### Task 3: 配置模型多账户化 + 兼容迁移

**Files:**
- Modify: `src/DeepSeekBalanceMonitor/Core/Config.cs`（加 `AccountConfig` 类、`Accounts`、`ActiveAccountId`、`ActiveAccount` 属性；删 `ApiKey`/`WarnThreshold`）
- Modify: `src/DeepSeekBalanceMonitor/Core/ConfigService.cs`（逐账户加密存储 + 旧格式迁移）
- Modify: `src/DeepSeekBalanceMonitor/AppContext.cs`（从 `Config.Accounts` 取首个账户创建 Monitor）
- Modify: `src/DeepSeekBalanceMonitor/UI/SettingsForm.cs`（`Config.ApiKey`/`Config.WarnThreshold` 引用改为 ActiveAccount）
- Modify: `src/DeepSeekBalanceMonitor/UI/StatsForm.cs`（`cfg.WarnThreshold` 引用改为 ActiveAccount）
- Modify: `src/DeepSeekBalanceMonitor/Core/AlertEngine.cs`（`Config.WarnThreshold` 引用改为 monitor 账户阈值）
- Test: `tools/CoreTests/Program.cs`

**Interfaces:**
- Consumes: `IBalanceProvider`（Task 1）、`ProviderRegistry`（Task 1）
- Produces:
  - `AccountConfig { string Id; string Name; string ProviderId; string ApiKey; decimal WarnThreshold; }`
  - `Config.Accounts : List<AccountConfig>`、`Config.ActiveAccountId : string`、`Config.ActiveAccount : AccountConfig`（无账户或找不到时 null）

> 本任务后 `BalanceMonitor` 仍用旧构造函数（单 key/阈值），AppContext 从 ActiveAccount 取值传入。

- [ ] **Step 1: 写失败测试** — `Main()` 中加 `TestConfigMigration();`：

```csharp
private static void TestConfigMigration()
{
    Console.WriteLine("-- 配置迁移 --");
    var path = Path.Combine(Path.GetTempPath(), "CoreTests-" + Guid.NewGuid().ToString("N") + ".json");
    try
    {
        // 先存一份新格式（含一个账户），把账户密文提出来
        var svc = new ConfigService(path);
        var cfg = new Config();
        cfg.Accounts.Add(new AccountConfig
        {
            Id = "acc1", Name = "默认账户", ProviderId = "deepseek",
            ApiKey = "sk-test-migrate", WarnThreshold = 20m
        });
        svc.Save(cfg);

        // 用反射调私有 EncryptKey 构造旧格式密文
        var enc = typeof(ConfigService).GetMethod("EncryptKey",
            System.Reflection.BindingFlags.Static | System.Reflection.BindingFlags.NonPublic)
            .Invoke(null, new object[] { "sk-test-migrate" });

        // 写旧格式：只有 ApiKeyEncrypted + WarnThreshold，无 Accounts
        var legacy = "{\"WarnThreshold\":20,\"ApiKeyEncrypted\":\"" + enc + "\",\"FontSize\":20}";
        File.WriteAllText(path, legacy);

        var loaded = new ConfigService(path).Load();
        Assert(loaded.Accounts.Count == 1, "旧格式迁移出 1 个账户");
        Assert(loaded.Accounts[0].ProviderId == "deepseek", "迁移账户为 deepseek");
        Assert(loaded.Accounts[0].ApiKey == "sk-test-migrate", "迁移保留密钥明文");
        Assert(loaded.Accounts[0].WarnThreshold == 20m, "迁移保留阈值");
        Assert(loaded.ActiveAccountId == loaded.Accounts[0].Id, "ActiveAccountId 指向迁移账户");

        // 新格式往返
        var svc2 = new ConfigService(path);
        var cfg2 = new Config();
        cfg2.Accounts.Add(new AccountConfig { Id = "a", Name = "A", ProviderId = "openrouter", ApiKey = "k1" });
        cfg2.Accounts.Add(new AccountConfig { Id = "b", Name = "B", ProviderId = "zai", ApiKey = "k2" });
        cfg2.ActiveAccountId = "b";
        svc2.Save(cfg2);
        var loaded2 = new ConfigService(path).Load();
        Assert(loaded2.Accounts.Count == 2 && loaded2.Accounts[0].ApiKey == "k1" && loaded2.Accounts[1].ApiKey == "k2",
            "多账户往返");
        Assert(loaded2.ActiveAccountId == "b", "ActiveAccountId 往返");
        Assert(!File.ReadAllText(path).Contains("k1") && !File.ReadAllText(path).Contains("k2"),
            "明文密钥不落盘");
    }
    finally { try { File.Delete(path); } catch { } }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 编译失败（`AccountConfig`/`Config.Accounts` 不存在）。

- [ ] **Step 3: 改造 Config.cs**

新增 `AccountConfig` 类 + `Config` 增删字段：

```csharp
/// <summary>一个被监控的账户（供应商 + 密钥 + 独立预警阈值）。</summary>
public class AccountConfig
{
    /// <summary>稳定主键（Guid）。</summary>
    public string Id { get; set; } = Guid.NewGuid().ToString("N");

    /// <summary>自定义名称（如「工作号」）。</summary>
    public string Name { get; set; } = "";

    /// <summary>供应商 Id（对应 ProviderRegistry）。</summary>
    public string ProviderId { get; set; } = "deepseek";

    /// <summary>API 密钥（内存明文，落盘前由 ConfigService 加密）。</summary>
    public string ApiKey { get; set; } = "";

    /// <summary>该账户余额预警阈值。</summary>
    public decimal WarnThreshold { get; set; } = 10m;
}
```

`Config` 中删除 `ApiKey`、`WarnThreshold` 两个属性，新增：

```csharp
/// <summary>全部被监控账户。</summary>
public List<AccountConfig> Accounts { get; set; } = new List<AccountConfig>();

/// <summary>悬浮窗当前显示的账户 Id。</summary>
public string ActiveAccountId { get; set; } = "";

/// <summary>当前账户（找不到时返回 null）。</summary>
[System.Web.Script.Serialization.ScriptIgnore]
public AccountConfig ActiveAccount
{
    get { return Accounts.Find(a => a.Id == ActiveAccountId) ?? (Accounts.Count > 0 ? Accounts[0] : null); }
}
```

> 注意：`ActiveAccount` 是只读计算属性，`JavaScriptSerializer` 序列化时会包含它——用 `[ScriptIgnore]` 标注（`using System.Web.Script.Serialization;`），避免 JSON 中出现冗余字段：

- [ ] **Step 4: 改造 ConfigService.cs**

`ConfigDocument` 增改：

```csharp
public List<AccountItem> Accounts { get; set; } = new List<AccountItem>();
// 当前显示的账户 Id
public string ActiveAccountId { get; set; } = "";
// 保留旧字段 ApiKeyEncrypted / WarnThreshold 用于迁移判断

public class AccountItem
{
    public string Id { get; set; }
    public string Name { get; set; }
    public string ProviderId { get; set; }
    public string ApiKeyEncrypted { get; set; } = "";
    public decimal WarnThreshold { get; set; }
}
```

`FromConfig`：序列化 `Accounts`（逐账户加密，忽略 Config.ActiveAccount 计算属性）：

```csharp
public static ConfigDocument FromConfig(Config c)
{
    var doc = new ConfigDocument { /* 现有字段不变 */ };
    doc.Accounts = (c.Accounts ?? new List<AccountConfig>()).Select(a => new AccountItem
    {
        Id = a.Id,
        Name = a.Name,
        ProviderId = a.ProviderId,
        ApiKeyEncrypted = EncryptKey(a.ApiKey),
        WarnThreshold = a.WarnThreshold
    }).ToList();
    doc.ActiveAccountId = c.ActiveAccountId;
    return doc;
}
```

`ToConfig`：解密账户列表 + 兜底 ActiveAccountId：

```csharp
public Config ToConfig()
{
    var cfg = new Config { /* 现有字段不变 */ };
    cfg.Accounts = (Accounts ?? new List<AccountItem>())
        .Where(a => !string.IsNullOrEmpty(a.Id))
        .Select(a => new AccountConfig
        {
            Id = a.Id,
            Name = string.IsNullOrEmpty(a.Name) ? a.ProviderId : a.Name,
            ProviderId = string.IsNullOrEmpty(a.ProviderId) ? "deepseek" : a.ProviderId,
            ApiKey = DecryptKey(a.ApiKeyEncrypted),
            WarnThreshold = a.WarnThreshold < 0 ? 10m : a.WarnThreshold
        }).ToList();
    cfg.ActiveAccountId = ActiveAccountId;
    if (string.IsNullOrEmpty(cfg.ActiveAccountId) && cfg.Accounts.Count > 0)
        cfg.ActiveAccountId = cfg.Accounts[0].Id;
    return cfg;
}
```

`Load()` 末尾加迁移（`doc != null` 分支内，`ToConfig()` 之前用旧字段判断）：

```csharp
// 旧版本迁移：有旧密钥且无账户 → 生成默认 DeepSeek 账户
if ((doc.Accounts == null || doc.Accounts.Count == 0)
    && !string.IsNullOrEmpty(doc.ApiKeyEncrypted))
{
    var acc = new AccountConfig
    {
        Name = "默认账户",
        ProviderId = "deepseek",
        ApiKey = DecryptKey(doc.ApiKeyEncrypted),
        WarnThreshold = doc.WarnThreshold < 0 ? 10m : doc.WarnThreshold
    };
    cfg.Accounts.Add(acc);
    cfg.ActiveAccountId = acc.Id;
    Save(cfg); // 立即落盘为新格式
    return cfg;
}
```

> 注意：`AccountItem`、`ActiveAccountId` 字段加入 `ConfigDocument`；`EncryptKey/DecryptKey` 保持 private（测试用反射访问）。

- [ ] **Step 5: 修所有旧引用**

`AppContext.cs` 构造中：

```csharp
// 原：Monitor = new BalanceMonitor(Api, History, Config.ApiKey, Config.WarnThreshold);
var act = Config.ActiveAccount;
Monitor = new BalanceMonitor(Api, History,
    act == null ? "" : act.ApiKey,
    act == null ? 10m : act.WarnThreshold);
```

`SettingsForm.cs` 旧引用替换（`_ctx.Config.ApiKey` → `_ctx.Config.ActiveAccount?.ApiKey ?? ""`；`_ctx.Config.WarnThreshold` → `_ctx.Config.ActiveAccount?.WarnThreshold ?? 10m`）。涉及行：231、197/208、369-371、382-384。同步改 `_ctx.Monitor.SetApiKey(key)` / `SetWarnThreshold` 调用改为在账户对象上修改后调用 monitor 方法（本任务保持 `_ctx.Monitor.SetApiKey/SetWarnThreshold` 不变，仅换数据来源）。

`StatsForm.cs` 第 206 行 `cfg.WarnThreshold` → `cfg.ActiveAccount?.WarnThreshold ?? 10m`。

`AlertEngine.cs` 第 35、39 行 `_ctx.Config.WarnThreshold` → `_ctx.Monitor` 对应账户阈值：

```csharp
var threshold = _ctx.Monitor != null && _ctx.Monitor.Balance.HasValue
    ? _ctx.Config.ActiveAccount?.WarnThreshold ?? 10m : 10m;
```

（AlertEngine 完整按账户改造在 Task 7。）

- [ ] **Step 6: 构建 + 测试**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release; if ($?) { dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe } }`
Expected: 主项目编译通过，测试全 PASS。

- [ ] **Step 7: 提交**

```bash
git add src/DeepSeekBalanceMonitor/Core/Config.cs src/DeepSeekBalanceMonitor/Core/ConfigService.cs src/DeepSeekBalanceMonitor/AppContext.cs src/DeepSeekBalanceMonitor/UI/SettingsForm.cs src/DeepSeekBalanceMonitor/UI/StatsForm.cs src/DeepSeekBalanceMonitor/Core/AlertEngine.cs tools/CoreTests/Program.cs
git commit -m "feat: 配置多账户化（AccountConfig + 兼容迁移）"
```

---

### Task 4: 历史存储按账户隔离

**Files:**
- Modify: `src/DeepSeekBalanceMonitor/Core/HistoryStore.cs`
- Modify: `src/DeepSeekBalanceMonitor/Core/BalanceMonitor.cs`（`Append` 调用带 AccountId）
- Modify: `src/DeepSeekBalanceMonitor/AppContext.cs`（构造 HistoryStore 传旧记录归属账户）
- Modify: `src/DeepSeekBalanceMonitor/UI/StatsForm.cs`、`Core/AlertEngine.cs`（统计调用带账户）
- Test: `tools/CoreTests/Program.cs`

**Interfaces:**
- Consumes: `AccountConfig.Id`（Task 3）
- Produces:
  - `BalanceRecord.AccountId : string`
  - `HistoryStore(string path, string legacyAccountId = null)`（构造时把无账户标记的旧记录归入该账户）
  - `Append(string accountId, decimal balance, DateTime time)`；`GetRecords(string accountId)`；`SpentSince(string accountId, DateTime from)`；`TodaySpent(string accountId)`；`AverageDailySpent(string accountId, int days)`；`TotalSpent(string accountId)`
  - 删除旧无参重载（调用方全部更新）

- [ ] **Step 1: 写失败测试** — 重写 `TestHistoryStore()`（多账户隔离 + 旧记录归属）：

```csharp
private static void TestHistoryStore()
{
    Console.WriteLine("-- 历史存储（按账户） --");
    var path = Path.Combine(Path.GetTempPath(), "CoreTests-" + Guid.NewGuid().ToString("N") + ".json");
    try
    {
        // 旧记录归属测试：先写无账户标记的文件，构造时归入 "acc1"
        var legacyPath = Path.Combine(Path.GetTempPath(), "CoreTests-" + Guid.NewGuid().ToString("N") + ".json");
        File.WriteAllText(legacyPath, "{\"records\":[{\"time\":\"2026-08-01 10:00:00\",\"balance\":100}]}");
        var storeL = new HistoryStore(legacyPath, "acc1");
        Assert(storeL.Records.Count == 1 && storeL.Records[0].AccountId == "acc1", "旧记录归属迁移账户");
        try { File.Delete(legacyPath); } catch { }

        var store = new HistoryStore(path);
        var day0 = DateTime.Today.AddDays(-6).AddHours(10);

        // 账户 A：100 → 95 → 90（三天），今天 85
        store.Append("accA", 100m, day0);
        store.Append("accA", 95m, day0.AddDays(1));
        store.Append("accA", 90m, day0.AddDays(2));
        store.Append("accA", 85m, DateTime.Today.AddHours(9));
        // 账户 B：50 → 49（今天）
        store.Append("accB", 50m, day0);
        store.Append("accB", 49m, DateTime.Today.AddHours(8));

        Assert(store.Records.Count == 6, "两个账户共 6 条");
        Assert(store.GetRecords("accA").Count == 4 && store.GetRecords("accB").Count == 2, "按账户过滤");

        // 同账户相同余额去重：只刷新时间戳
        store.Append("accA", 85m, DateTime.Today.AddHours(10));
        Assert(store.GetRecords("accA").Count == 4, "同账户相同余额去重");

        // 消费统计按账户
        Assert(store.TodaySpent("accA") == 5m, "账户A 今日消费 5");
        Assert(store.TodaySpent("accB") == 1m, "账户B 今日消费 1");
        Assert(store.TotalSpent("accA") == 15m, "账户A 总消费 15");
        Assert(store.TotalSpent("accB") == 1m, "账户B 总消费 1");
        Assert(store.AverageDailySpent("accA", 7) == 10m / 3m, "账户A 近7天日均（不含今天）");

        // 持久化重载
        var store2 = new HistoryStore(path);
        Assert(store2.GetRecords("accA").Count == 4 && store2.GetRecords("accB").Count == 2, "重载后按账户一致");
        Assert(store2.TodaySpent("accA") == 5m, "重载后账户A 今日消费一致");

        // 损坏回退
        File.WriteAllText(path, "{broken json");
        var store3 = new HistoryStore(path);
        Assert(store3.Records.Count == 0, "损坏文件安全回退");
    }
    finally { try { File.Delete(path); } catch { } }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 编译失败（签名不匹配）。

- [ ] **Step 3: 改造 HistoryStore.cs**

`BalanceRecord` 加 `AccountId`；`Append` 带账户参数；统计/过滤方法带账户参数；构造加 `legacyAccountId`：

```csharp
public class BalanceRecord
{
    public DateTime Time { get; set; }
    public decimal Balance { get; set; }
    public string AccountId { get; set; } = "";
}
```

`Append`（按账户找最后一条去重）：

```csharp
public void Append(string accountId, decimal balance, DateTime time)
{
    lock (_lock)
    {
        var last = _records.Count > 0 ? _records.LastOrDefault(r => r.AccountId == accountId) : null;
        if (last != null && last.Balance == balance) { last.Time = time; SaveToDisk(); return; }
        _records.Add(new BalanceRecord { Time = time, Balance = balance, AccountId = accountId });
        if (_records.Count > MaxRecords)
            _records = _records.Skip(_records.Count - MaxRecords / 2).ToList();
        SaveToDisk();
    }
}
```

过滤与统计（原无参方法改为带 accountId 参数，内部按 `GetRecords(accountId)` 计算）：

```csharp
public IReadOnlyList<BalanceRecord> Records { get { lock (_lock) { return _records.ToList(); } } }

public IReadOnlyList<BalanceRecord> GetRecords(string accountId)
{
    lock (_lock) { return _records.Where(r => r.AccountId == accountId).ToList(); }
}

public decimal TotalSpent(string accountId) => SpentSince(accountId, DateTime.MinValue);

public decimal SpentSince(string accountId, DateTime from)
{
    lock (_lock)
    {
        var rs = _records.Where(r => r.AccountId == accountId).ToList();
        decimal spent = 0;
        for (int i = 1; i < rs.Count; i++)
        {
            if (rs[i].Time < from) continue;
            var d = rs[i - 1].Balance - rs[i].Balance;
            if (d > 0) spent += d;
        }
        return spent;
    }
}

public decimal TodaySpent(string accountId) => SpentSince(accountId, DateTime.Today);

public decimal AverageDailySpent(string accountId, int days)
{
    lock (_lock)
    {
        var rs = _records.Where(r => r.AccountId == accountId).ToList();
        var today = DateTime.Today;
        var start = today.AddDays(-days);
        decimal spent = 0; int daysWithData = 0;
        for (int i = 1; i < rs.Count; i++)
        {
            var t = rs[i].Time;
            if (t < start || t >= today) continue;
            var d = rs[i - 1].Balance - rs[i].Balance;
            if (d > 0) spent += d;
        }
        for (int i = 0; i < rs.Count; i++)
        {
            var t = rs[i].Time;
            if (t < start || t >= today) continue;
            if (i == 0 || rs[i - 1].Time.Date != t.Date) daysWithData++;
        }
        return daysWithData == 0 ? 0m : spent / daysWithData;
    }
}
```

构造与 `LoadFromDisk`（旧记录无 AccountId 时归入 legacyAccountId）：

```csharp
private readonly string _legacyAccountId;

public HistoryStore(string path, string legacyAccountId = null)
{
    _path = path;
    _legacyAccountId = legacyAccountId ?? "";
    _records = LoadFromDisk();
}
```

`LoadFromDisk` 里 `Select` 时：

```csharp
.Select(r => new BalanceRecord
{
    Time = r.time,
    Balance = r.balance,
    AccountId = string.IsNullOrEmpty(r.accountId) ? _legacyAccountId : r.accountId
}).ToList();
```

`HistoryDoc` / `RecordItem` 加 `accountId` 字段；`SaveToDisk` 输出 `accountId = r.AccountId`。

- [ ] **Step 4: 修调用方**

`BalanceMonitor.cs`：`_history.Append(...)` → `_history.Append(_accountId, result.TotalBalance, result.Time)`，其中 `_accountId` 为新增字段（本任务先加 `private string _accountId = "";`，在 Task 5 改为从账户读；暂时固定为 `""`）。

`AppContext.cs`：`History = new HistoryStore(HistoryPath)` → 传入旧记录归属账户：

```csharp
var migrateAccountId = (Config.Accounts.Count > 0 && string.IsNullOrEmpty(Config.Accounts[0].ProviderId) == false)
    ? Config.Accounts[0].Id : null;
History = new HistoryStore(HistoryPath, Config.Accounts.Count > 0 ? Config.Accounts[0].Id : null);
```

`StatsForm.cs`：`history.TodaySpent()` / `history.TotalSpent()` / `history.Records` 改为带当前账户 id：`var aid = _ctx.Config.ActiveAccount?.Id ?? "";`，统计用 `history.TodaySpent(aid)`、`history.TotalSpent(aid)`、`history.GetRecords(aid)`。

`AlertEngine.cs`：`_ctx.History.TodaySpent()` → `_ctx.History.TodaySpent(_ctx.Config.ActiveAccount?.Id ?? "")`；`AverageDailySpent(7)` 同理。

- [ ] **Step 5: 构建 + 测试**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release; if ($?) { dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe } }`
Expected: 编译通过，测试全 PASS。

- [ ] **Step 6: 提交**

```bash
git add src/DeepSeekBalanceMonitor/Core/HistoryStore.cs src/DeepSeekBalanceMonitor/Core/BalanceMonitor.cs src/DeepSeekBalanceMonitor/AppContext.cs src/DeepSeekBalanceMonitor/UI/StatsForm.cs src/DeepSeekBalanceMonitor/Core/AlertEngine.cs tools/CoreTests/Program.cs
git commit -m "feat: 历史记录按账户隔离"
```

---

### Task 5: BalanceMonitor 多实例化（provider + account）

**Files:**
- Modify: `src/DeepSeekBalanceMonitor/Core/BalanceMonitor.cs`（构造函数、AccountId、SetApiKey/SetWarnThreshold 改账户）
- Modify: `src/DeepSeekBalanceMonitor/AppContext.cs`（用 `ProviderRegistry.Get` + `AccountConfig` 构造）
- Modify: `src/DeepSeekBalanceMonitor/UI/SettingsForm.cs`（`SetApiKey`/`SetWarnThreshold` 行为变化无需改，验证编译）
- Test: `tools/CoreTests/Program.cs`

**Interfaces:**
- Consumes: `IBalanceProvider`（Task 1）、`AccountConfig`（Task 3）、`HistoryStore.Append(string, decimal, DateTime)`（Task 4）
- Produces:
  - `BalanceMonitor(IBalanceProvider provider, HistoryStore history, AccountConfig account)`
  - `string AccountId`、`string AccountName`、`decimal WarnThreshold`、`decimal? Balance`、`BalanceStatus Status`、`int ConsecutiveFailures`、`string ErrorMessage`
  - `void SetApiKey(string)`、`void SetWarnThreshold(decimal)`、`void SetInterval(int)`、`void RefreshNow()`、`Task RefreshNowAsync()`
  - `event EventHandler StateChanged`

- [ ] **Step 1: 写失败测试** — `Main()` 中加 `TestBalanceMonitor();`（用内存假 provider，不联网）：

```csharp
/// <summary>假 provider：固定余额，记录最近查询的 key。</summary>
private class FakeProvider : IBalanceProvider
{
    public string Id => "fake";
    public string DisplayName => "Fake";
    public string BaseUrl => "http://fake";
    public decimal Value = 50m;
    public string LastKey;

    public Task<AccountBalance> GetBalanceAsync(string apiKey)
    {
        LastKey = apiKey;
        return Task.FromResult(new AccountBalance { IsAvailable = true, Remaining = Value, Currency = "CNY" });
    }
}

private static void TestBalanceMonitor()
{
    Console.WriteLine("-- BalanceMonitor（多实例） --");
    var path = Path.Combine(Path.GetTempPath(), "CoreTests-" + Guid.NewGuid().ToString("N") + ".json");
    try
    {
        var fp = new FakeProvider { Value = 100m };
        var acc = new AccountConfig { Id = "acc1", Name = "一号", ProviderId = "fake", ApiKey = "k1", WarnThreshold = 30m };
        var mon = new BalanceMonitor(fp, new HistoryStore(path), acc);
        mon.RefreshNowAsync().Wait();

        Assert(mon.Balance == 100m && mon.Status == BalanceStatus.Normal, "查询成功且余额充足为 Normal");
        Assert(mon.AccountId == "acc1" && mon.AccountName == "一号", "账户信息透出");
        Assert(fp.LastKey == "k1", "使用账户的 API Key");

        // 阈值：余额低于阈值 → Low
        fp.Value = 10m;
        mon.SetWarnThreshold(30m);
        mon.RefreshNowAsync().Wait();
        Assert(mon.Status == BalanceStatus.Low, "余额低于阈值为 Low");

        // 错误：provider 抛异常 → Error，保留最后余额
        var bad = new ThrowingProvider();
        var mon2 = new BalanceMonitor(bad, new HistoryStore(path), new AccountConfig { Id = "acc2", ApiKey = "k2" });
        mon2.RefreshNowAsync().Wait();
        Assert(mon2.Status == BalanceStatus.Error && mon2.ConsecutiveFailures == 1, "查询失败为 Error");
    }
    finally { try { File.Delete(path); } catch { } }
}

private class ThrowingProvider : IBalanceProvider
{
    public string Id => "bad"; public string DisplayName => "Bad"; public string BaseUrl => "http://bad";
    public Task<AccountBalance> GetBalanceAsync(string apiKey)
        => throw new BalanceQueryException(QueryErrorKind.Network, "超时");
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 编译失败（新构造签名不存在）。

- [ ] **Step 3: 改造 BalanceMonitor.cs**

构造函数与字段：

```csharp
private readonly IBalanceProvider _provider;
private readonly AccountConfig _account;
private readonly HistoryStore _history;

public string AccountId => _account.Id;
public string AccountName => _account.Name;
public string ProviderDisplayName => _provider.DisplayName;

public BalanceMonitor(IBalanceProvider provider, HistoryStore history, AccountConfig account)
{
    _provider = provider;
    _history = history;
    _account = account;

    _timer = new Timer();
    _timer.Tick += OnTick;
    _timer.Interval = 30000;
}
```

`RefreshAsync` 中：

```csharp
var result = await _provider.GetBalanceAsync(_account.ApiKey).ConfigureAwait(true);
Balance = result.Remaining;
...
_history.Append(_account.Id, result.TotalBalance ?? result.Remaining ?? 0m, result.Time);
```

> 余额取 `Remaining`；`result.TotalBalance` 已不存在（AccountBalance 无该字段），历史记录统一存 `Remaining ?? Total ?? 0m`。

`SetApiKey` / `SetWarnThreshold`：

```csharp
public void SetApiKey(string apiKey) { _account.ApiKey = apiKey ?? ""; _ = RefreshAsync(); }
public void SetWarnThreshold(decimal threshold) { _account.WarnThreshold = threshold; ReevaluateStatus(); }
```

`ReevaluateStatus` 用 `_account.WarnThreshold`。新增：

```csharp
public Task RefreshNowAsync() => RefreshAsync();
```

`RefreshAsync` 改为 `private async Task` 返回 Task（保留防重入/退避逻辑，`_configuredInterval` 字段保留）。

- [ ] **Step 4: 改 AppContext.cs**

```csharp
// 原：Monitor = new BalanceMonitor(Api, History, act?.ApiKey ?? "", act?.WarnThreshold ?? 10m);
var act = Config.ActiveAccount;
var provider = act == null ? null : ProviderRegistry.Get(act.ProviderId);
Monitor = new BalanceMonitor(provider ?? new DeepSeekProvider(), History, act ?? new AccountConfig { ProviderId = "deepseek" });
```

> `provider ?? new DeepSeekProvider()` 兜底：账户缺失或未知供应商时仍能启动（状态为错误待配置）。`SettingsForm` 的 `_ctx.Api` 测试密钥功能在 Task 8 改为按账户 provider 查询。

- [ ] **Step 5: 构建 + 测试**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release; if ($?) { dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe } }`
Expected: 编译通过，测试全 PASS。

- [ ] **Step 6: 提交**

```bash
git add src/DeepSeekBalanceMonitor/Core/BalanceMonitor.cs src/DeepSeekBalanceMonitor/AppContext.cs tools/CoreTests/Program.cs
git commit -m "refactor: BalanceMonitor 适配 provider + 账户"
```

---

### Task 6: MonitorCoordinator 多账户调度

**Files:**
- Create: `src/DeepSeekBalanceMonitor/Core/MonitorCoordinator.cs`
- Modify: `src/DeepSeekBalanceMonitor/AppContext.cs`（MonitorCoordinator 替代单 Monitor；`Monitor` 属性改为协调器当前账户）
- Modify: `src/DeepSeekBalanceMonitor/Core/AlertEngine.cs`、`UI/FloatingWindow.cs`、`UI/TrayIcon.cs`、`UI/StatsForm.cs`（`_ctx.Monitor` → `_ctx.Coordinator.Current`）
- Test: `tools/CoreTests/Program.cs`

**Interfaces:**
- Consumes: `BalanceMonitor`（Task 5）、`AccountConfig`（Task 3）
- Produces:
  - `MonitorCoordinator : IDisposable` — `SetAccounts(IEnumerable<AccountConfig>, HistoryStore)`、`string ActiveAccountId { get; set; }`、`BalanceMonitor Current`、`BalanceMonitor Get(string accountId)`、`IReadOnlyList<BalanceMonitor> Monitors`、`void SetInterval(int)`、`void SetWarnThreshold(string accountId, decimal)`、`void SetApiKey(string accountId, string)`、`void RefreshNow()`、`event EventHandler StateChanged`、`Start()`/`Stop()`/`Dispose()`

- [ ] **Step 1: 写失败测试** — `Main()` 中加 `TestCoordinator();`：

```csharp
private static void TestCoordinator()
{
    Console.WriteLine("-- MonitorCoordinator --");
    var path = Path.Combine(Path.GetTempPath(), "CoreTests-" + Guid.NewGuid().ToString("N") + ".json");
    try
    {
        var fpA = new FakeProvider { Value = 100m };
        var fpB = new FakeProvider { Value = 5m };
        var coord = new MonitorCoordinator();
        coord.SetAccounts(new[]
        {
            new AccountConfig { Id = "a", Name = "A", ProviderId = "fake", ApiKey = "ka", WarnThreshold = 10m },
            new AccountConfig { Id = "b", Name = "B", ProviderId = "fake", ApiKey = "kb", WarnThreshold = 10m }
        }, new HistoryStore(path), id => id == "a" ? fpA : (IBalanceProvider)fpB);

        coord.ActiveAccountId = "a";
        coord.RefreshNow();
        System.Threading.Thread.Sleep(300);

        var ma = coord.Get("a");
        Assert(ma != null && ma.Balance == 100m && ma.Status == BalanceStatus.Normal, "账户A 查询成功");
        var mb = coord.Get("b");
        Assert(mb != null && mb.Balance == 5m && mb.Status == BalanceStatus.Low, "账户B 低余额为 Low");

        Assert(coord.Current != null && coord.Current.AccountId == "a", "Current 指向当前账户");
        coord.ActiveAccountId = "b";
        Assert(coord.Current.AccountId == "b", "切换后 Current 更新");

        // 事件聚合：某账户状态变化触发协调器事件
        int fired = 0;
        coord.StateChanged += (s, e) => fired++;
        fpA.Value = 1m;
        coord.Get("a").RefreshNowAsync().Wait();
        Assert(fired >= 1, "协调器聚合账户状态事件");

        // SetInterval 广播
        coord.SetInterval(60); // 不抛异常即通过（内部广播到各账户）
    }
    finally { try { File.Delete(path); } catch { } }
}
```

> `BalanceMonitor.Start(intervalSeconds)` 已是公开方法（现有签名）。本任务不依赖 `GetEffectiveIntervalMs`。

- [ ] **Step 2: 运行测试验证失败**

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using System.Windows.Forms;

namespace DeepSeekBalanceMonitor.Core
{
    /// <summary>
    /// 多账户监控协调器：每个账户一个 <see cref="BalanceMonitor"/> 实例，
    /// 统一管理生命周期与事件，按 ActiveAccountId 提供当前账户。
    /// </summary>
    public class MonitorCoordinator : IDisposable
    {
        private readonly List<BalanceMonitor> _monitors = new List<BalanceMonitor>();
        private readonly object _lock = new object();
        private string _activeAccountId = "";

        /// <summary>任一账户状态变化时触发（含当前账户切换）。</summary>
        public event EventHandler StateChanged;

        /// <summary>全部账户监控实例。</summary>
        public IReadOnlyList<BalanceMonitor> Monitors
        {
            get { lock (_lock) { return _monitors.ToList(); } }
        }

        /// <summary>当前显示的账户 Id。</summary>
        public string ActiveAccountId
        {
            get { return _activeAccountId; }
            set
            {
                _activeAccountId = value;
                StateChanged?.Invoke(this, EventArgs.Empty);
            }
        }

        /// <summary>当前账户监控实例（找不到返回 null）。</summary>
        public BalanceMonitor Current => Get(_activeAccountId) ?? Monitors.FirstOrDefault();

        /// <summary>按账户 Id 查找监控实例。</summary>
        public BalanceMonitor Get(string accountId)
        {
            lock (_lock) { return _monitors.FirstOrDefault(m => m.AccountId == accountId); }
        }

        /// <summary>
        /// 用账户列表重建监控实例（设置窗口增删账户后调用）。
        /// providerResolver: 供应商 Id → 适配器实例。
        /// </summary>
        public void SetAccounts(IEnumerable<AccountConfig> accounts, HistoryStore history,
            Func<string, IBalanceProvider> providerResolver)
        {
            lock (_lock)
            {
                foreach (var m in _monitors) { m.StateChanged -= OnMonitorChanged; m.Dispose(); }
                _monitors.Clear();

                foreach (var acc in accounts)
                {
                    var provider = providerResolver(acc.ProviderId);
                    if (provider == null) continue;
                    var mon = new BalanceMonitor(provider, history, acc);
                    mon.StateChanged += OnMonitorChanged;
                    _monitors.Add(mon);
                }
                if (string.IsNullOrEmpty(_activeAccountId) || Get(_activeAccountId) == null)
                    _activeAccountId = _monitors.FirstOrDefault()?.AccountId ?? "";
            }
            StateChanged?.Invoke(this, EventArgs.Empty);
        }

        private void OnMonitorChanged(object sender, EventArgs e)
        {
            StateChanged?.Invoke(this, e);
        }

        /// <summary>启动全部账户轮询。</summary>
        public void Start()
        {
            foreach (var m in Monitors) m.Start(30);
        }

        /// <summary>停止全部账户轮询。</summary>
        public void Stop()
        {
            foreach (var m in Monitors) m.Stop();
        }

        /// <summary>广播刷新间隔（秒）。</summary>
        public void SetInterval(int intervalSeconds)
        {
            foreach (var m in Monitors) m.SetInterval(intervalSeconds);
        }

        /// <summary>修改某账户预警阈值。</summary>
        public void SetWarnThreshold(string accountId, decimal threshold)
        {
            Get(accountId)?.SetWarnThreshold(threshold);
        }

        /// <summary>修改某账户 API 密钥并立即查询。</summary>
        public void SetApiKey(string accountId, string apiKey)
        {
            Get(accountId)?.SetApiKey(apiKey);
        }

        /// <summary>立即刷新全部账户（异步，不阻塞）。</summary>
        public void RefreshNow()
        {
            foreach (var m in Monitors) m.RefreshNow();
        }

        public void Dispose()
        {
            foreach (var m in Monitors) m.Dispose();
        }
    }
}
```

> `BalanceMonitor.Start(intervalSeconds)` 已是公开方法（现有签名）。本任务不依赖 `GetEffectiveIntervalMs`。

- [ ] **Step 4: 改 AppContext.cs**

替换 Monitor 组装块：

```csharp
Coordinator = new MonitorCoordinator();
Coordinator.SetAccounts(Config.Accounts, History,
    pid => ProviderRegistry.Get(pid));
Coordinator.ActiveAccountId = Config.ActiveAccountId;
```

新增公开属性（替代原 `Monitor` 属性，供 UI 使用）：

```csharp
public MonitorCoordinator Coordinator { get; private set; }

/// <summary>当前账户监控实例（兼容旧 UI 引用，无账户时返回 null）。</summary>
public BalanceMonitor Monitor => Coordinator?.Current;
```

`Monitor.StateChanged` 订阅改为 `Coordinator.StateChanged`（原有三处 `Monitor.StateChanged +=` 改为 `Coordinator.StateChanged +=`；`Coordinator.StateChanged` 在账户切换时也会触发，UI 刷新逻辑天然覆盖）。

`Monitor.Start(Config.RefreshIntervalSeconds)` → `Coordinator.Start(); Coordinator.SetInterval(Config.RefreshIntervalSeconds);`

`ExitApp` 中 `Monitor?.Stop()` → `Coordinator?.Stop()`。

- [ ] **Step 5: 修 UI 引用（`_ctx.Monitor` → `_ctx.Coordinator.Current`，并做 null 安全）**

- `UI/FloatingWindow.cs` `UpdateDisplay()`：`var m = _ctx.Monitor;` → `var m = _ctx.Coordinator?.Current;`；加 `if (m == null) { _balanceText = "未配置账户"; _balanceTextColor = 灰; }` 提前返回。
- `UI/TrayIcon.cs` `UpdateFromMonitor()` / `RefreshMenu()`：`_ctx.Monitor` → `_ctx.Coordinator?.Current`，加 null 守卫（无账户时显示默认/暂无数据，不崩）。
- `UI/StatsForm.cs` `RefreshData()`：`var monitor = _ctx.Monitor;` → `var monitor = _ctx.Coordinator?.Current;`；账户 id 取 `monitor?.AccountId ?? ""`。
- `Core/AlertEngine.cs` `OnBalanceChanged()`：`var m = _ctx.Monitor;` → `var m = _ctx.Coordinator?.Current;`，`if (m == null) return;` 提前。

- [ ] **Step 6: 构建 + 测试**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release; if ($?) { dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe } }`
Expected: 编译通过，测试全 PASS。

- [ ] **Step 7: 提交**

```bash
git add src/DeepSeekBalanceMonitor/Core/MonitorCoordinator.cs src/DeepSeekBalanceMonitor/AppContext.cs src/DeepSeekBalanceMonitor/UI/FloatingWindow.cs src/DeepSeekBalanceMonitor/UI/TrayIcon.cs src/DeepSeekBalanceMonitor/UI/StatsForm.cs src/DeepSeekBalanceMonitor/Core/AlertEngine.cs tools/CoreTests/Program.cs
git commit -m "feat: MonitorCoordinator 多账户调度与当前账户切换"
```

---

### Task 7: AlertEngine 按账户归属

**Files:**
- Modify: `src/DeepSeekBalanceMonitor/Core/AlertEngine.cs`（`_wasLow`/`_lastSurgeDay` 改为按账户字典；消费统计按账户）
- Modify: `src/DeepSeekBalanceMonitor/AppContext.cs`（订阅 `Coordinator.StateChanged` 传当前 monitor）
- Test: `tools/CoreTests/Program.cs`

**Interfaces:**
- Consumes: `MonitorCoordinator.Current`（Task 6）、`HistoryStore.TodaySpent(aid)/AverageDailySpent(aid, days)`（Task 4）
- Produces: `AlertEngine.OnBalanceChanged(BalanceMonitor monitor)`（公开，参数为变化账户的 monitor；不传则用当前账户）

- [ ] **Step 1: 写失败测试** — `Main()` 中加 `TestAlertEngine();`：

```csharp
private static void TestAlertEngine()
{
    Console.WriteLine("-- AlertEngine（按账户） --");
    var path = Path.Combine(Path.GetTempPath(), "CoreTests-" + Guid.NewGuid().ToString("N") + ".json");
    try
    {
        var history = new HistoryStore(path);
        var cfg = new Config();
        cfg.NotifyLowBalance = true;
        int lowNotify = 0, surgeNotify = 0;
        var alert = new AlertEngine(cfg, history,
            (title, msg, icon) => { if (title.Contains("余额不足")) lowNotify++; if (title.Contains("消费突增")) surgeNotify++; });

        var accA = new AccountConfig { Id = "a", Name = "A", ProviderId = "fake", ApiKey = "ka", WarnThreshold = 30m };
        var accB = new AccountConfig { Id = "b", Name = "B", ProviderId = "fake", ApiKey = "kb", WarnThreshold = 30m };
        var fpA = new FakeProvider { Value = 50m };
        var fpB = new FakeProvider { Value = 50m };
        var monA = new BalanceMonitor(fpA, history, accA);
        var monB = new BalanceMonitor(fpB, history, accB);

        // 初始化（首次不提醒）
        alert.OnBalanceChanged(monA);
        Assert(lowNotify == 0, "首次状态不提醒");

        // 账户A 跌到阈值以下 → 提醒一次
        fpA.Value = 10m;
        monA.RefreshNowAsync().Wait();
        alert.OnBalanceChanged(monA);
        Assert(lowNotify == 1, "A 低余额提醒");
        alert.OnBalanceChanged(monA);
        Assert(lowNotify == 1, "A 连续低余额不重复提醒");

        // 账户B 也跌到阈值以下 → 独立提醒（按账户隔离）
        fpB.Value = 10m;
        monB.RefreshNowAsync().Wait();
        alert.OnBalanceChanged(monB);
        Assert(lowNotify == 2, "B 低余额独立提醒");

        // A 恢复后再次跌破 → 再提醒
        fpA.Value = 50m;
        monA.RefreshNowAsync().Wait();
        alert.OnBalanceChanged(monA);
        fpA.Value = 5m;
        monA.RefreshNowAsync().Wait();
        alert.OnBalanceChanged(monA);
        Assert(lowNotify == 3, "A 恢复后再跌破重新提醒");
    }
    finally { try { File.Delete(path); } catch { } }
}
```

> `AlertEngine` 构造函数改为可注入 `Action<string, string, ToolTipIcon> notify`，`ToolTipIcon` 在 `System.Windows.Forms`。AppContext 里传 `(t, m, i) => Notify.Show(t, m, i)`。

- [ ] **Step 2: 运行测试验证失败**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 编译失败（新构造/方法签名）。

- [ ] **Step 3: 改造 AlertEngine.cs**

```csharp
public class AlertEngine
{
    private readonly Config _config;
    private readonly HistoryStore _history;
    private readonly Action<string, string, ToolTipIcon> _notify;

    private readonly Dictionary<string, bool> _wasLow = new Dictionary<string, bool>();
    private readonly Dictionary<string, DateTime> _lastSurgeDay = new Dictionary<string, DateTime>();

    public AlertEngine(Config config, HistoryStore history, Action<string, string, ToolTipIcon> notify)
    {
        _config = config;
        _history = history;
        _notify = notify;
    }

    /// <summary>某账户监控状态变化时调用（账户切换时传当前 monitor）。</summary>
    public void OnBalanceChanged(BalanceMonitor monitor)
    {
        if (monitor == null) return;
        var aid = monitor.AccountId;
        var threshold = monitor.WarnThreshold;

        if (monitor.Status == BalanceStatus.Low)
        {
            bool wasLow = _wasLow.TryGetValue(aid, out var w) && w;
            if (_config.NotifyLowBalance && !wasLow && monitor.Balance.HasValue)
            {
                _notify("⚠ 余额不足",
                    monitor.AccountName + " 余额仅剩 ¥" + monitor.Balance.Value.ToString("F2")
                    + "（预警阈值: ¥" + threshold.ToString("F2") + "）",
                    ToolTipIcon.Warning);
            }
            _wasLow[aid] = true;
        }
        else if (monitor.Status == BalanceStatus.Normal)
        {
            _wasLow[aid] = false;
        }

        if (monitor.Balance.HasValue) CheckSurge(monitor, aid);
    }

    private void CheckSurge(BalanceMonitor monitor, string accountId)
    {
        if (!_config.NotifySurge) return;
        if (_lastSurgeDay.TryGetValue(accountId, out var day) && day == DateTime.Today) return;

        var today = _history.TodaySpent(accountId);
        var avg = _history.AverageDailySpent(accountId, 7);
        if (avg <= 0m || today <= 0m) return;

        if (today > avg * 3)
        {
            _notify("🔥 消费突增",
                monitor.AccountName + " 今日消费 ¥" + today.ToString("F2")
                + "，超过近 7 天日均消费（¥" + avg.ToString("F2") + "）的 3 倍，请注意排查异常消耗！",
                ToolTipIcon.Warning);
            _lastSurgeDay[accountId] = DateTime.Today;
        }
    }
}
```

`Config` 已无 `NotifyLowBalance`/`NotifySurge`？——仍在（Task 3 只删了 ApiKey/WarnThreshold）。确认：`NotifyLowBalance`/`NotifySurge` 保留全局开关。

`BalanceMonitor` 加 `public decimal WarnThreshold => _account.WarnThreshold;`（供 AlertEngine 读取；在 Task 5 引入 `_account` 时一并加入，Task 7 直接使用）。

- [ ] **Step 4: 改 AppContext.cs**

```csharp
// 原：Alerts = new AlertEngine(this);
Alerts = new AlertEngine(Config, History, (t, m, i) => Notify.Show(t, m, i));
Coordinator.StateChanged += (s, e) => Alerts.OnBalanceChanged(Coordinator.Current);
```

> 原 `Monitor.StateChanged += (s, e) => Alerts.OnBalanceChanged();` 删除（已在 Task 6 改为 Coordinator 事件，这里替换处理函数签名）。

- [ ] **Step 5: 构建 + 测试**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release; if ($?) { dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe } }`
Expected: 编译通过，测试全 PASS。

- [ ] **Step 6: 提交**

```bash
git add src/DeepSeekBalanceMonitor/Core/AlertEngine.cs src/DeepSeekBalanceMonitor/Core/BalanceMonitor.cs src/DeepSeekBalanceMonitor/AppContext.cs tools/CoreTests/Program.cs
git commit -m "feat: 告警按账户隔离"
```

---

### Task 8: 设置窗口账户管理区（列表 + 编辑区）

**Files:**
- Modify: `src/DeepSeekBalanceMonitor/UI/SettingsForm.cs`（新增「账户」组：账户列表 + 编辑区；删旧「API 密钥」单输入组）
- Modify: `src/DeepSeekBalanceMonitor/AppContext.cs`（删 `Api` 属性；`ShowSettings` 无需改）
- Delete: `src/DeepSeekBalanceMonitor/Core/DeepSeekApiClient.cs`（逻辑已迁入 DeepSeekProvider，AppContext.Api 删除后无引用）
- Modify: `tools/CoreTests/Program.cs`（删 `TestBalanceParse`——已由 `TestDeepSeekProvider` 覆盖）

**Interfaces:**
- Consumes: `Config.Accounts`、`Config.ActiveAccountId`、`ProviderRegistry.All`（Task 1）、`MonitorCoordinator.SetAccounts`（Task 6）
- Produces: 无新公共接口（UI 内部）

- [ ] **Step 1: 运行现有测试确认基线**

Run: `dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe }`
Expected: 全 PASS（本任务无失败测试先行——UI 手工验证，见 Step 5）。

- [ ] **Step 2: 删 DeepSeekApiClient + 清理引用**

- 删除 `src/DeepSeekBalanceMonitor/Core/DeepSeekApiClient.cs`。
- `AppContext.cs`：删 `public DeepSeekApiClient Api { get; }` 及 `Api = new DeepSeekApiClient();`。
- `tools/CoreTests/Program.cs`：删 `TestBalanceParse()` 方法与 `Main()` 中的调用（`TestDeepSeekProvider` 已覆盖）。

- [ ] **Step 3: 重写 SettingsForm 的密钥组为账户管理组**

删除 `BuildKeyGroup()` 及其调用，新增：

```csharp
private ListBox _accountList;
private TextBox _nameBox, _keyBox;
private ComboBox _providerBox;
private NumericUpDown _thresholdBox;
private Button _btnTest, _btnApply, _btnDelete, _btnAdd;
private Label _lblKeyStatus;
private string _editingId; // 当前编辑账户 Id
```

`BuildAccountsGroup()` 布局（组高约 260，宽 436）：

- 左列：`_accountList`（Location (14,26)，Size (130, 190)，DisplayMember 用账户名）
- 右列编辑区（x=160 起）：
  - `名称：` Label + `_nameBox` (260 宽)
  - `供应商：` Label + `_providerBox`（ComboBox，Items = ProviderRegistry.All 的 DisplayName，Tag 存 Id）
  - `API 密钥：` Label + `_keyBox`（UseSystemPasswordChar + 「显示」CheckBox）
  - `预警阈值：` Label + `_thresholdBox`（NumericUpDown 0~99999.99，2 位小数）+ 「元」
  - 按钮行：`_btnAdd`「新增账户」/ `_btnDelete`「删除」/ `_btnTest`「测试」/ `_btnApply`「应用」
  - `_lblKeyStatus` 状态行

事件：
- `_accountList.SelectedIndexChanged` → 保存当前编辑到账户对象 → 加载选中账户到编辑区
- `_btnAdd` → 生成新 `AccountConfig`（Id=Guid，Name="新账户"，ProviderId="deepseek"）加入 `Config.Accounts`，刷新列表并选中，`ApplyAccounts()`
- `_btnDelete` → 确认后从 Accounts 删除；若删的是 ActiveAccountId，ActiveAccountId 指向剩余首个；`ApplyAccounts()`
- `_btnTest` → `await ProviderRegistry.Get(选中.ProviderId).GetBalanceAsync(key)`，成功显示余额，失败显示错误
- `_btnApply` → 名称/供应商/密钥/阈值写回账户对象 → `ApplyAccounts()` + 立即查询
- 编辑区任何控件变化 → `MarkDirty()`（现有防抖保存）

`ApplyAccounts()`：

```csharp
private void ApplyAccounts()
{
    _ctx.Coordinator.SetAccounts(_ctx.Config.Accounts, _ctx.History,
        pid => ProviderRegistry.Get(pid));
    _ctx.Coordinator.ActiveAccountId = _ctx.Config.ActiveAccountId;
    _ctx.Coordinator.SetInterval(_ctx.Config.RefreshIntervalSeconds);
    _ctx.SaveConfig();
}
```

「余额提醒」组：删阈值 NumericUpDown，保留两个通知开关。

> 注意 `SettingsForm` 现有 `AddGroup/FinishGroup/AddCheck` 布局辅助沿用；`_y` 游标、滚动、暗色主题、AutoScroll 保持。`ClientSize` 高度按内容增长。

- [ ] **Step 4: 删除旧密钥引用残留**

全文件搜索 `_ctx.Api`、`_ctx.Config.ApiKey`、`_ctx.Config.WarnThreshold`、`_ctx.Monitor.SetApiKey` 清理。`SettingsForm` 中原 `OnTestKey/OnApplyKey/OnClearKey` 重写为账户编辑语义。

- [ ] **Step 5: 构建 + 手工验证清单**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release; if ($?) { dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe } }`

手工验证（启动 `src\DeepSeekBalanceMonitor\bin\Release\net48\DeepSeekBalanceMonitor.exe`）：
1. 新增/删除账户、改名、切供应商、改阈值即时生效
2. 测试密钥功能可用（DeepSeek 真 key）
3. 旧配置（有密钥的老用户）首次启动自动迁移出一个「默认账户」
4. 无账户时悬浮窗显示「未配置账户」

- [ ] **Step 6: 提交**

```bash
git add src/DeepSeekBalanceMonitor/UI/SettingsForm.cs src/DeepSeekBalanceMonitor/AppContext.cs tools/CoreTests/Program.cs
git commit -m "feat: 设置窗口多账户管理（列表+编辑区）"
```

> 删除文件：`git rm src/DeepSeekBalanceMonitor/Core/DeepSeekApiClient.cs`（单独一行，随本任务一起）。

---

### Task 9: 托盘账户子菜单 + 悬浮窗账户名

**Files:**
- Modify: `src/DeepSeekBalanceMonitor/UI/TrayIcon.cs`（「账户」子菜单）
- Modify: `src/DeepSeekBalanceMonitor/UI/FloatingWindow.cs`（显示账户名行）
- Modify: `src/DeepSeekBalanceMonitor/AppContext.cs`（切换账户入口方法）

**Interfaces:**
- Consumes: `MonitorCoordinator.ActiveAccountId`、`Config.Accounts`（Task 3/6）
- Produces:
  - `AppContext.SwitchAccount(string accountId)`（切 ActiveAccountId + SaveConfig + 协调器刷新）
  - `TrayIcon.RefreshMenu()` 中重建账户子菜单项
  - `FloatingWindow.UpdateDisplay()` 顶部账户名行

- [ ] **Step 1: 手工前置确认**（无失败测试——UI 行为，Step 4 手工验证）

- [ ] **Step 2: AppContext 加切换入口**

```csharp
/// <summary>切换当前显示的账户（托盘「账户」子菜单调用）。</summary>
public void SwitchAccount(string accountId)
{
    Config.ActiveAccountId = accountId;
    SaveConfig();
    Coordinator.ActiveAccountId = accountId; // 触发 StateChanged → 悬浮窗/托盘/统计刷新
}
```

- [ ] **Step 3: TrayIcon 加账户子菜单**

构造中新增 `_miAccounts`（放在「统计」上方）：

```csharp
_miAccounts = new ToolStripMenuItem("账户");
_menu.Items.Add(_miAccounts);
```

`RefreshMenu()` 中重建子项（每次打开菜单前刷新）：

```csharp
_miAccounts.DropDownItems.Clear();
foreach (var acc in _ctx.Config.Accounts)
{
    var name = acc.Name;
    if (string.IsNullOrEmpty(name)) name = _ctx.Coordinator.Get(acc.Id)?.ProviderDisplayName ?? acc.ProviderId;
    var item = new ToolStripMenuItem(name) { Checked = acc.Id == _ctx.Config.ActiveAccountId };
    string aid = acc.Id;
    item.Click += (s, e) => _ctx.SwitchAccount(aid);
    _miAccounts.DropDownItems.Add(item);
}
_miAccounts.Visible = _ctx.Config.Accounts.Count > 1;
```

> 无账户或仅 1 个账户时隐藏「账户」菜单。

- [ ] **Step 4: FloatingWindow 显示账户名**

`FloatingWindow.cs`：
- 加字段 `private string _accountNameText = "";`
- `UpdateDisplay()` 中（null 分支外）：

```csharp
_accountNameText = m == null ? "" : m.AccountName;
```

- `UpdateSize()` 改两行测量：宽 = max(账户名行宽, 余额行宽) + 8；高 = 账户名行高 + 余额行高 + 6（有账户名时）。
- `OnPaint` 画两行：账户名（小字号 9f，灰色）在上，余额主字在下；无账户名时只画余额。

```csharp
if (!string.IsNullOrEmpty(_accountNameText))
{
    var nameFont = new Font("Microsoft YaHei UI", 9f);
    var nameRect = new Rectangle(0, 2, Width, 18);
    TextRenderer.DrawText(e.Graphics, _accountNameText, nameFont, nameRect, Color.FromArgb(0x9C, 0x9C, 0x9C), flags);
    nameFont.Dispose();
}
TextRenderer.DrawText(e.Graphics, _balanceText, _balanceFont,
    new Rectangle(0, 18, Width, Height - 18), _balanceTextColor, flags);
```

- `ApplyConfig()` / `UpdateDisplay()` 末尾调用 `UpdateSize()` 已存在，尺寸变化自动适配。

- [ ] **Step 5: 构建 + 手工验证**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release`

手工验证：配置 2+ 账户 → 托盘「账户」子菜单出现且勾选当前 → 点击切换悬浮窗余额与账户名联动变化；单账户时菜单隐藏；悬浮窗无截断。

- [ ] **Step 6: 提交**

```bash
git add src/DeepSeekBalanceMonitor/UI/TrayIcon.cs src/DeepSeekBalanceMonitor/UI/FloatingWindow.cs src/DeepSeekBalanceMonitor/AppContext.cs
git commit -m "feat: 托盘账户切换菜单 + 悬浮窗账户名显示"
```

---

### Task 10: 统计面板按当前账户

**Files:**
- Modify: `src/DeepSeekBalanceMonitor/UI/StatsForm.cs`

**Interfaces:**
- Consumes: `MonitorCoordinator.Current`（Task 6）、`HistoryStore` 账户方法（Task 4）、`Config.ActiveAccount`（Task 3）

- [ ] **Step 1: 手工前置确认**（无失败测试——UI 行为，Step 3 手工验证）

- [ ] **Step 2: 改造 RefreshData()**

```csharp
public void RefreshData()
{
    var monitor = _ctx.Coordinator?.Current;
    if (monitor == null) { Text = "统计 - DeepSeek 余额监控"; return; }
    var aid = monitor.AccountId;
    var history = _ctx.History;
    var records = history.GetRecords(aid);
    var cfg = _ctx.Config;
    var threshold = monitor.WarnThreshold;

    // 标题显示当前账户
    Text = "统计 - " + monitor.AccountName + "（" + monitor.ProviderDisplayName + "）";

    string fp = (records.Count > 0 ? records[records.Count - 1].Time.Ticks + "|" + records[records.Count - 1].Balance : "e")
        + "|" + monitor.Balance + "|" + threshold + "|" + aid;
    if (fp == _dataFingerprint) return;
    _dataFingerprint = fp;

    decimal? balance = monitor.Balance;
    decimal today = history.TodaySpent(aid);
    decimal total = history.TotalSpent(aid);
    // 其余逻辑同现有，但用 records / total / today 的账户版本
    ...
}
```

（表头/行渲染逻辑不变，数据源换为账户过滤版本。）

- [ ] **Step 3: 构建 + 手工验证**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release`

手工验证：打开统计面板 → 标题显示当前账户名；托盘切换账户 → 统计刷新为对应账户数据；无账户时面板空态不崩。

- [ ] **Step 4: 提交**

```bash
git add src/DeepSeekBalanceMonitor/UI/StatsForm.cs
git commit -m "feat: 统计面板按当前账户过滤"
```

---

### Task 11: 全量验证 + 文档更新

**Files:**
- Modify: `docs/技术架构.md`（更新项目结构、多账户设计）
- Modify: `README.md`（功能描述补多供应商/多账户）
- Modify: `docs/superpowers/specs/2026-08-17-多账户余额监控-design.md`（如实现与设计有出入，记录实现偏差）

- [ ] **Step 1: 全量构建 + 测试**

Run: `dotnet build src/DeepSeekBalanceMonitor/DeepSeekBalanceMonitor.csproj -c Release; if ($?) { dotnet build tools/CoreTests/CoreTests.csproj -c Release; if ($?) { tools\CoreTests\bin\Release\net48\CoreTests.exe } }`
Expected: 编译通过，全部测试 PASS，退出码 0。

- [ ] **Step 2: 冒烟运行**

Run: `src\DeepSeekBalanceMonitor\bin\Release\net48\DeepSeekBalanceMonitor.exe`
验证：启动无崩溃、托盘图标出现、悬浮窗显示当前账户余额或「未配置账户」。

- [ ] **Step 3: 更新文档**

- `docs/技术架构.md`：项目结构树补 `Core/Providers/` 与 `MonitorCoordinator.cs`；第 4 节补「多账户模型」小节（AccountConfig、协调器、切换交互）。
- `README.md`：功能一览补「多供应商支持（DeepSeek/OpenRouter/Moonshot/Z.ai）」与「多账户切换」；数据文件描述补账户字段。
- 若实现与 spec 有偏差（如保留每账户 Timer 而非单定时器），在 spec 文件追加「实现偏差」一节说明。

- [ ] **Step 4: 提交**

```bash
git add docs/技术架构.md README.md docs/superpowers/specs/2026-08-17-多账户余额监控-design.md
git commit -m "docs: 多账户余额监控文档更新"
```

---

## 自审记录（写入计划时执行）

- **Spec 覆盖**：适配器层（Task 1-2）✓；配置模型+迁移（Task 3）✓；历史按账户（Task 4）✓；监控多实例+协调器（Task 5-6）✓；告警按账户（Task 7）✓；设置窗口账户管理（Task 8）✓；托盘切换+悬浮窗账户名（Task 9）✓；统计过滤（Task 10）✓；测试（各任务内嵌）✓。
- **占位符扫描**：无 TBD/TODO；每个代码步骤给出实际代码。
- **类型一致性**：`AccountBalance.Remaining` 为 `decimal?`；`AccountConfig.Id/Name/ProviderId/ApiKey` 为 string；`BalanceRecord.AccountId` 为 string；`BalanceMonitor.AccountId` 由 `_account.Id` 提供；`AlertEngine.OnBalanceChanged(BalanceMonitor)` 签名在 Task 5 测试与 Task 7 实现一致；`ProviderRegistry.Get(string)` 全任务一致。
- **实现偏差说明**：spec 第 3 节「单定时器驱动」→ 实际保留每账户 `Timer`（Global Constraints 已声明，理由：复用现有退避/防重入，改动面最小）。Task 11 Step 3 在 spec 记录该偏差。